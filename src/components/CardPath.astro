---
export interface Props {
  image: string;
  title: string;
  body: string;
  button: string;
}

const { title, body, image, button } = Astro.props;
---

<figure class="Path__card">
  <img src={image} alt={title} />
  <figcaption>
    {title}
  </figcaption>
  <p class="Path__card--body">
    {body}
  </p>
  <footer class="footer-card">
    <button class="btn btn-blue">{button}</button>
  </footer>
</figure>
